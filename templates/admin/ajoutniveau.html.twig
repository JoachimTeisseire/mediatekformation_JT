{% extends "adminbasefront.html.twig" %}
{% block body %}
    <p class="mt-3">
        <h3>Ajouter un niveau</h3>
    </p>
    {{ form_start(formniveau) }}
        {{ form_widget(formniveau) }}
    {{ form_end(formniveau) }}
    
{% endblock %}{# empty Twig template #}
